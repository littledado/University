#include <stdio.h>
#include <string.h>
#include <stdlib.h>


char *cercaRegexp(char*src, char *regexp);

int main(){



    return EXIT_SUCCESS;
}

char *cercaRegexp(char*src, char *regexp){
    int regExpLen = calculateRegexpLen(regexp);
    
}